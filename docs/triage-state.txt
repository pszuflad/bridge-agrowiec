# Marker triażu zmian produkcji (komenda /triaz-zmian).
# Pełny SHA ostatnio striażowanego commita sync(vps) NA GAŁĘZI main (tam pisze producent).
# Następny triaż: `git fetch origin main && git log <ten SHA>..origin/main | grep 'sync(vps)'`.
08be0f326d6b6bc66782ecf0f9dafb01e4e9eb59
# ^ 2026-08-25 16:00 „uwagacena" — ostatni sync producenta. Wszystko do tego commita striażowane
#   (backlog #1–#6 + wpisy sesji odbudowy). PO 25.08 brak nowych sync(vps) — patrz nota niżej.
#
# 2026-09-08: origin/main stoi na 72957d7 (25.08); od 14 dni ZERO nowych sync(vps).
#   Do ustalenia, czy to feature-freeze pod cutover, czy zatrzymany cron producenta.

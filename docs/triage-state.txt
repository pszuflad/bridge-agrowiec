# Marker triażu zmian produkcji (komenda /triaz-zmian).
# Pełny SHA ostatnio striażowanego commita sync(vps) NA GAŁĘZI main (tam pisze producent).
# Następny triaż: `git fetch origin main && git log <ten SHA>..origin/main | grep 'sync(vps)'`.
94bdf11
# ^ 2026-09-09. Bieżący HEAD main. STRIAŻOWANE 2026-09-09 (w ramach 46-CHORE-revert-13d1-selly).
#   Zakres d88ac15..94bdf11 = 3 commity sync(vps) (08-09 17:00, 09-09 06:00, 09-09 09:00).
#   WSZYSTKO to docieranie podsystemu Selly (Tor 2): sync_full przepisany (428 linii, usunięte PUT
#   features po Selly 400), mapper_v2 v2.1 (bez vat_rate), discovery.buildProductCodeCache, weryfikacja
#   pierwszego nocnego Tor 2 (MO5 1713 ok). ZERO zmian kodu FE/menu — tag [FRONTEND] w mailu to tylko
#   przegenerowany sellycsv-*.csv (dane). NIE tworzymy nowej karty: to należy do ODŁOŻONEGO 13d
#   (roadmapa blok 13d — zegar zresetowany).
#   Wcześniej striażowane 6872aea: backlog #53–#64 + domknięcia #3/#8/#9/#10; plan I13 (13a–13f).
#   Następny triaż: git log 94bdf11..origin/main | grep 'sync(vps)'.
#
# PRODUCENT ZNÓW DZIAŁA (2026-09-09): fix d88ac15 zadziałał — te 3 commity przyszły z automatu + mail.
#
# ZMIANA PRODUCENTA (2026-09-08): cron vps-sync.sh MILCZAŁ od 25.08 do 08.09 — grep BAKS
#   `\.bak_pre_` zwracał 1 przy nowym nazewnictwie .bak Ani (.bak_katunify_/.bak-2026-…/.bak_b10_/
#   .bak_bug2_) i pod set -euo pipefail ubijał skrypt przed git commit/push. NAPRAWIONE w d88ac15
#   (wzorzec .bak rozszerzony + `|| true` na pipeline BAKS i DIFF). Producent wróci do automatu
#   przy najbliższym cronie. DO ZROBIENIA RĘCZNIE: potwierdzić, że cron w panelu DirectAdmin nadal
#   istnieje i że pierwszy przebieg po fixie przechodzi (mail + brak nowego commita = OK, bo
#   6872aea ma już wszystko).
#
# --- poprzedni marker (do 25.08) ---
# 08be0f326d6b6bc66782ecf0f9dafb01e4e9eb59  2026-08-25 16:00 „uwagacena"
#   Wszystko do niego striażowane (backlog #1–#6 + wpisy sesji odbudowy).
